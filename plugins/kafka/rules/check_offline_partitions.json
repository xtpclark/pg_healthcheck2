{
  "critical_offline_partitions": {
    "metric_keywords": ["check_offline_partitions", "data"],
    "data_conditions": [
      {"key": "offline_count", "exists": true}
    ],
    "rules": [{
      "expression": "data.get('offline_count', 0) > 0",
      "level": "critical",
      "score": 10,
      "reasoning": "🔴 SEVERE DATA OUTAGE: {data.get('offline_count')} offline partition(s) - data completely unavailable!",
      "recommendations": [
        "IMMEDIATE ACTION REQUIRED - Data is completely inaccessible",
        "Identify offline partitions: kafka-topics.sh --unavailable-partitions",
        "Check broker status - Are all replica brokers running?",
        "Review broker logs for crashes or errors",
        "Check network connectivity between brokers",
        "Verify disk health on all brokers",
        "If all replicas down, consider unclean leader election (CAUSES DATA LOSS)"
      ]
    }]
  }
}
