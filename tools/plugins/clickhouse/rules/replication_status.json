{
  "check": "replication_status",
  "rules": [
    {
      "condition": "structured_data['replication_status']['status'] == 'success' and any(row.get('active', 0) == 0 for row in structured_data['replication_status']['data'])",
      "alert_level": "CRITICAL",
      "message": "Inactive replicas detected. Data consistency across nodes may be compromised."
    },
    {
      "condition": "structured_data['queue_backlog']['status'] == 'success' and any(row.get('queue_size', 0) > 100 for row in structured_data['queue_backlog']['data'])",
      "alert_level": "WARNING",
      "message": "Significant replication queue backlog detected. Delays in data replication may occur."
    }
  ]
}