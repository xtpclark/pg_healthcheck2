{
  "check_module": "query_log_latency",
  "rules": [
    {
      "id": "high_latency_queries",
      "description": "Warn if queries have high latency",
      "condition": {
        "path": "query_log_analysis.data",
        "check": "any(item['query_duration_ms'] > 1000 for item in value)"
      },
      "severity": "warning",
      "message": "High latency queries detected. Consider optimization or indexing."
    }
  ]
}
